name: .NET WinForms Build & Release

on:
  push:
    branches: [ "master" ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        configuration: [Release]

    env:
      Project_Path: LibreLinkUp_Windows.csproj
      App_Name: LibreLinkUp_Windows

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    # Clean project
    - name: Clean project
      run: dotnet clean ${{ env.Project_Path }}

    # Restore dependencies
    - name: Restore dependencies
      run: dotnet restore ${{ env.Project_Path }}

    # Build project
    - name: Build project
      run: dotnet build ${{ env.Project_Path }} --configuration ${{ matrix.configuration }} --no-restore

    # ==========================
    # 1️⃣ Portable ZIP build
    # ==========================
    - name: Publish portable build
      run: dotnet publish ${{ env.Project_Path }} --configuration ${{ matrix.configuration }} --output ./publish/portable

    - name: Create ZIP (Portable)
      run: Compress-Archive -Path ./publish/portable/* -DestinationPath ${{ env.App_Name }}-Portable.zip

    # ==========================
    # 2️⃣ Self-contained Installer (EXE)
    # ==========================
    - name: Publish self-contained (installer)
      run: dotnet publish ${{ env.Project_Path }} --configuration ${{ matrix.configuration }} --runtime win-x64 --self-contained true --output ./publish/selfcontained

    - name: Create ZIP (Installer)
      run: Compress-Archive -Path ./publish/selfcontained/* -DestinationPath ${{ env.App_Name }}-Installer.zip

    # Verify outputs
    - name: List published files
      run: |
        dir ./publish/portable
        dir ./publish/selfcontained

    # ==========================
    # Upload to GitHub Release
    # ==========================
    - name: Upload build artifacts to release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          ${{ env.App_Name }}-Portable.zip
          ${{ env.App_Name }}-Installer.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
